import type { ModelCatalogEntry } from './types'

export const FALLBACK_MODELS: readonly ModelCatalogEntry[] = []

export const fallbackModels = FALLBACK_MODELS
export const FALLBACK_MODEL_CATALOG = FALLBACK_MODELS
